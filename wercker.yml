box: node
build:
    steps:
        - npm-install
        - npm-test
        - script:
            name: echo nodejs information
            code: |
                echo "node version $(node -v) running"
                echo "npm version $(npm -v) running"
        - script:
            name: version incriment and build and commit
            code: |
                npm run verup-patch
                npm run build
                npm run git-push